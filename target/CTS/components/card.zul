<?xml version="1.0" encoding="UTF-8"?>
<zk>
    <vlayout sclass="card"
        onClick='if("${arg.url}" != "") {Executions.sendRedirect("${arg.url}")}'>
        <div sclass='card-icon icon-${arg.iconKey}'/>
        <label value="${arg.title}" sclass="card-text"/>
    </vlayout>
</zk>
